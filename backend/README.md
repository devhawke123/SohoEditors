# Soho Editors — Backend

FastAPI service shared by the Training and (later) Talent websites. See root `../architecture.md` for the full system design.

Not yet scaffolded — this folder currently holds only config placeholders (`.env.example`). Scaffolding (FastAPI app, DB models/migrations, auth) comes next.
