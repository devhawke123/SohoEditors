# Soho Editors — Training Website: Design

> Placeholder — to be filled in once Figma screens are shared. Structure below anticipates what will land here so agents know where to look/add.

## Status

No design files received yet. Nothing in this document is final.

## What Will Go Here (once Figma is provided)

- Design system tokens: colors, type scale, spacing, shared components (buttons, forms, cards, nav) — feeds the shared component library described in `architecture.md`.
- Per-page layouts for the fixed/static pages: Training Home, Courses, Course Detail, Our Trainers, Resources, Free Tutorials, Reviews, Certified Pro Tutorials, Live Online, Bespoke.
- Cart / checkout flow screens (cart, attendee capture, coupon, VAT breakdown, PayPal handoff, confirmation).
- Pencil Me In UI (customer-facing state + admin confirm/release tooling).
- Admin panel screens (catalogue CRUD, bookings, users, settings).
- Responsive behavior notes (desktop / tablet / mobile) per `architecture.md` §3.

## How to Update This File

When Figma screens are shared:
1. Link the Figma file(s) here.
2. Extract and record tokens/components as they're finalized (not before — avoid documenting in-flux designs as if final).
3. Note explicitly which pages are "fixed layout, CMS content" vs. any exceptions — cross-check against the content/layout split in `rules.md` before accepting any one-off layout request.
